/**
 * Description: It divides the vaulue based on the given two inputs
 */
function divide(paramA, paramB) {
    var x = paramA;
    var y = paramB;
    var z = x / y;
    return z;
}
/**
 * Description: It divides the vaulue based on the given two inputs
 */
function divide(paramA, paramB) {
    var x = paramA;
    var y = paramB;
    var z = x / y;
    return z;
}